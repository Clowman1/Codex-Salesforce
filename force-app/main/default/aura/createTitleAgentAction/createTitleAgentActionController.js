({
    createTitleAgent: function(component, event, helper) {
        helper.createTitleAgent(component);
    },

    closeAction: function(component, event, helper) {
        helper.closeAction();
    }
})
