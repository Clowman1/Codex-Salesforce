<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Is_Realtor__c</fullName>
    <formula>OR(
    AND(
        NOT(ISBLANK(AccountId)),
        Account.Is_Realtor__c = TRUE
    ),
    AND(
        NOT(ISBLANK(AccountId)),
        INCLUDES(Account.Group__pc, &apos;Realtor&apos;)
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Is Realtor</label>
    <type>Checkbox</type>
</CustomField>
